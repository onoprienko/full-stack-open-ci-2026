name: Periodic Health Check

on:
  workflow_dispatch:
  # schedule:
  # - cron: '0 0 * * *'

jobs:
  health_check:
    runs-on: ubuntu-latest
    steps:
      - name: URL Health Check
        uses: Jtalk/url-health-check-action@v5
        with:
          url: https://full-stack-open-ci-2026.onrender.com/health
